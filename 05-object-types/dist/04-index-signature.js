"use strict";
//索引签名
const myArray = ['a', 'b'];
const secondItem = myArray[0];
let testString = {
    x: 100,
    y: 200,
    // aa:'aaa'
};
let notOkay = {
    x: 100,
    length: 100,
    name: 'felix'
};
let myArr = ['a', 'b'];
// myArr[0] = 'c' //myArr是readonly的，无法修改
